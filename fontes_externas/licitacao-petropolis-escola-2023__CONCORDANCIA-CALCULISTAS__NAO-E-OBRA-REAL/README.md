# Fonte: licitacao-petropolis-escola-2023__CONCORDANCIA-CALCULISTAS__NAO-E-OBRA-REAL - CONCORDANCIA ENTRE CALCULISTAS - NAO E OBRA CONSTRUIDA

> **CONCORDANCIA ENTRE CALCULISTAS - NAO E OBRA CONSTRUIDA**
> Esta obra e fonte externa para concordancia entre calculistas, nao obra edificada. Nao rotular como "validado contra obra real".

Este diretorio (`licitacao-petropolis-escola-2023__CONCORDANCIA-CALCULISTAS__NAO-E-OBRA-REAL`) e um dos **quatro lugares** do rotulo G24:

1. **Diretorio** — sufixo `__CONCORDANCIA-CALCULISTAS__NAO-E-OBRA-REAL` no nome
2. **JSON** — `_aviso` / `rotulo` em `fonte.json`, `fixture.json`, `comparacao.json`
3. **README** — este arquivo
4. **Relatorio** — `relatorio.txt` e `comparacao.json`

Classe: `licitacao_executada` (hierarquia: licitacao_executada > projeto_licitado > livro_exemplo_resolvido > tcc_academico > material_comercial)

PDF: `original.pdf` (SHA-256 verificado contra `registro.json`)
Fixture: `fixture.json` — cada valor com `pagina` + `trecho_literal` (guarda contra fabricacao)
Veredito: `comparacao.json` — enum fechado {framework_errado, fonte_errada, hipotese_divergente, nao_comparavel, nao_conclusivo}
